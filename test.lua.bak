function has_multiple_slashes_in_row(s)
	local i = 1
	for c in s:gmatch"." do
		if i ~= #s and string.sub(s,i,i) == "/" and string.sub(s,i+1,i+1) == "/" then
			return true
		end
		i = i+1
	end
	return false
end

if has_multiple_slashes_in_row("/l/") then
	print("true")
else
	print("false")
end
